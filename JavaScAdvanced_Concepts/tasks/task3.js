// JSON Conversion: Convert a JavaScript object to JSON format and log it to the console.
const user={
    name:'john',
    age:21,
    city:'NYK'
};
const newjson=JSON.stringify(user);
console.log(newjson);